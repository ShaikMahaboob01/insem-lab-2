apiVersion: v2
name: event-management
description: Helm chart for Event Management Fullstack Application with frontend and backend deployments, services, ingress, and configurable parameters.
type: application
version: 1.0.0
appVersion: "1.0"
